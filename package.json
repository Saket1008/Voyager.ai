{
  "name": "voyager-ai",
  "version": "1.0.0",
  "description": "Voyager AI - Intelligent Travel Itinerary Generator",
  "scripts": {
    "predev": "node scripts/kill-ports.js",
    "dev": "concurrently -k -n server,frontend -c blue,magenta \"npm run dev:server\" \"npm run dev:frontend\"",
    "dev:server": "cd server && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install-all": "npm install && cd server && npm install && cd ../frontend && npm install",
    "build": "cd frontend && npm run build",
    "start": "cd server && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "kill-port": "^2.0.1"
  }
}
